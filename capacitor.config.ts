import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter.crickpro",
  appName: "crickpro",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
